# دليل إعداد البريد الإلكتروني 📧

## نظرة عامة

تم تفعيل ميزة إرسال البريد الإلكتروني الحقيقي في نموذج "اتصل بنا". عندما يرسل زائر رسالة، سيتم إرسالها مباشرة إلى بريدك الإلكتروني.

## المتطلبات

1. حساب بريد إلكتروني (Gmail، Outlook، Yahoo، أو أي خدمة SMTP أخرى)
2. تفعيل SMTP في حسابك
3. إنشاء App Password (للأمان)

---

## الإعداد السريع

### 1️⃣ نسخ ملف الإعدادات

```bash
cp .env.example .env
```

### 2️⃣ تعديل ملف `.env`

افتح الملف وعدّل المعلومات التالية:

```env
# البريد الذي سيُرسل منه (حسابك)
SMTP_USERNAME=your-email@gmail.com

# كلمة المرور أو App Password
SMTP_PASSWORD=your-app-password-here

# البريد الذي سيستقبل الرسائل (قد يكون نفس البريد أو مختلف)
CONTACT_EMAIL_TO=admin@example.com
```

---

## إعداد Gmail (الخيار الأكثر شيوعاً)

### الخطوة 1: تفعيل التحقق بخطوتين

1. اذهب إلى [حساب Google](https://myaccount.google.com/)
2. اختر **الأمان** (Security)
3. فعّل **التحقق بخطوتين** (2-Step Verification)

### الخطوة 2: إنشاء App Password

1. في صفحة الأمان، ابحث عن **App passwords**
2. اختر **Mail** كالتطبيق
3. اختر **Other** واكتب "نبراس" كالجهاز
4. اضغط **Generate**
5. انسخ كلمة المرور المكونة من 16 حرف (مثل: `abcd efgh ijkl mnop`)

### الخطوة 3: تحديث ملف .env

```env
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=abcd efgh ijkl mnop
CONTACT_EMAIL_TO=your-email@gmail.com
```

> ⚠️ **مهم**: استخدم App Password وليس كلمة مرور حسابك الأساسية!

---

## إعداد Outlook/Hotmail

```env
SMTP_SERVER=smtp-mail.outlook.com
SMTP_PORT=587
SMTP_USERNAME=your-email@outlook.com
SMTP_PASSWORD=your-password
CONTACT_EMAIL_TO=your-email@outlook.com
```

---

## إعداد Yahoo Mail

### الخطوة 1: إنشاء App Password

1. اذهب إلى [Yahoo Account Security](https://login.yahoo.com/account/security)
2. اختر **Generate app password**
3. اختر **Other App** واكتب "نبراس"
4. انسخ كلمة المرور

### الخطوة 2: تحديث الإعدادات

```env
SMTP_SERVER=smtp.mail.yahoo.com
SMTP_PORT=587
SMTP_USERNAME=your-email@yahoo.com
SMTP_PASSWORD=your-app-password
CONTACT_EMAIL_TO=your-email@yahoo.com
```

---

## إعداد خدمات أخرى

### ProtonMail

```env
SMTP_SERVER=smtp.protonmail.com
SMTP_PORT=587
```

### Zoho Mail

```env
SMTP_SERVER=smtp.zoho.com
SMTP_PORT=587
```

### خادم SMTP مخصص

```env
SMTP_SERVER=mail.yourdomain.com
SMTP_PORT=587
SMTP_USERNAME=noreply@yourdomain.com
SMTP_PASSWORD=your-secure-password
```

---

## اختبار الإعداد

### 1. شغّل التطبيق

```bash
python main.py
```

### 2. ابحث في السجلات عن

عند بدء التشغيل، يجب أن ترى:

```
📧 Email Service: ✅ متصل وجاهز
```

إذا رأيت:
```
⚠️ Email Service: اتصال ضعيف
```

يعني أن هناك مشكلة في الإعدادات.

### 3. اختبار عملي

1. اذهب إلى صفحة "اتصل بنا" في الموقع
2. املأ النموذج وأرسل رسالة تجريبية
3. افحص بريدك الإلكتروني (تحقق من مجلد Spam أيضاً)

---

## حل المشاكل الشائعة 🔧

### ❌ خطأ: "فشل تسجيل الدخول إلى SMTP"

**الحل:**
1. تأكد من صحة البريد الإلكتروني
2. تأكد من استخدام App Password وليس كلمة المرور العادية
3. تأكد من تفعيل "Less secure app access" (للخدمات القديمة)

### ❌ خطأ: "Connection timeout"

**الحل:**
1. تحقق من اتصال الإنترنت
2. جرب port مختلف (465 بدلاً من 587)
3. تحقق من جدار الحماية (Firewall)

### ❌ الرسائل لا تصل

**الحل:**
1. تحقق من مجلد Spam/Junk
2. أضف البريد المُرسِل إلى جهات الاتصال
3. تحقق من سجلات التطبيق (logs)

### ❌ خطأ: "إعدادات البريد غير مُعيَّنة"

**الحل:**
تأكد من ملء هذه الحقول في `.env`:
- `SMTP_USERNAME`
- `SMTP_PASSWORD`
- `CONTACT_EMAIL_TO`

---

## الأمان 🔒

### ⚠️ نصائح مهمة:

1. **لا تشارك ملف `.env` أبداً** - أضفه إلى `.gitignore`
2. **استخدم App Passwords** بدلاً من كلمات المرور الأساسية
3. **فعّل التحقق بخطوتين** في حساب البريد
4. **غيّر كلمة المرور بانتظام**
5. **استخدم بريد منفصل** لإرسال الإشعارات (اختياري)

---

## شكل الرسالة المُستلمة 📬

عندما يرسل زائر رسالة، ستصلك على هذا الشكل:

```
من: نبراس - الأربعون النووية <your-email@gmail.com>
إلى: admin@example.com
الموضوع: [نبراس - اتصل بنا] استفسار عن الموقع
الرد على: visitor@example.com

─────────────────────────
📬 رسالة جديدة من نبراس
─────────────────────────

الاسم: أحمد محمد
البريد: visitor@example.com
الموضوع: استفسار عن الموقع

الرسالة:
السلام عليكم، أريد الاستفسار عن...

---
تم الإرسال في: 2024-02-14 18:30:00
```

---

## الدعم والمساعدة

إذا واجهت أي مشاكل:

1. راجع السجلات في Terminal
2. تأكد من صحة جميع الإعدادات
3. جرب خدمة بريد إلكتروني أخرى
4. اتصل بمزود خدمة البريد للدعم التقني

---

✅ بعد إتمام الإعداد، سيكون نموذج "اتصل بنا" جاهزاً لاستقبال الرسائل الحقيقية!
